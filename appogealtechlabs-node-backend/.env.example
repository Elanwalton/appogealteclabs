PORT=5000
NODE_ENV=development

# Firebase - download the serviceAccountKey.json from:
# Firebase Console > Project Settings > Service Accounts > Generate new private key
# Place it in the root of this folder as serviceAccountKey.json
